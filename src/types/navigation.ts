export interface NavigationItem {
    name: string;
    href: string;
  }
  
  export interface Language {
    code: string;
    label: string;
    flag: string;
  }
  
  export interface HeaderProps {
    className?: string;
  }